/**
 * Page size for the shop page's lazy-loaded menu grid.
 * 8 items = 2 rows on desktop (4 columns) / 4 rows on mobile (2 columns),
 * so one page ≈ a screenful plus the buffered row below it.
 */
export const MENU_PAGE_SIZE = 8;
